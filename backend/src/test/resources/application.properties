spring.datasource.url=jdbc:h2:mem:healthcaredb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Mock AI
llm.api.key=
llm.api.url=
llm.api.model=

# Mock Mail
spring.mail.host=localhost
spring.mail.port=25

# Disable JobRunr background server for tests to prevent Hikari connection closed errors
org.jobrunr.background-job-server.enabled=false
org.jobrunr.dashboard.enabled=false

google.client.id=dummy-test-id
google.client.secret=dummy-test-secret
